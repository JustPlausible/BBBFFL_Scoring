services:
  database:
    image: postgres:16
    environment:
      POSTGRES_DB: bbbffl_2026_second_half
      POSTGRES_USER: bbbffl
      POSTGRES_PASSWORD: second-half-only
    volumes: [second-half-database:/var/lib/postgresql/data]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bbbffl -d bbbffl_2026_second_half"]
      interval: 5s
      timeout: 5s
      retries: 12
  app:
    build: {context: ./bbbffl_app}
    env_file: [./bbbffl_app/.env.second-half-replay]
    ports: ["8019:8000"]
    volumes:
      - ./replay/2026-second-half/evidence:/replay/evidence:ro
      - ./replay/2026-second-half/state:/replay/state:ro
      - ./replay/2026-second-half/logs:/replay/logs
      - ./replay/2026-second-half/backups:/replay/backups
      - ./replay/2026-second-half/config:/secure:ro
    depends_on:
      database: {condition: service_healthy}
volumes:
  second-half-database:
